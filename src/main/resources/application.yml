spring:
  datasource:
    url: jdbc:mysql://localhost:3306/memory_seal?serverTimezone=Asia/Seoul
    username: root
    password: whalsrud12!!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true